# !/usr/bin/python
# coding=utf-8
"""Standalone service objects (settings, state, values, presets, icons,
shortcuts). Consumed compositionally by widgets, handlers, the bridge and
the Switchboard — see root ``uitk.__init__`` for public API."""
